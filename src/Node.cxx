#include <scripter/Node.h>

// This file is primarily empty because Node is an abstract base class
// with no concrete implementations.

// The ReturnValue struct is fully defined in the header file and
// doesn't require additional implementation.

// All concrete node implementations are in AST.cxx